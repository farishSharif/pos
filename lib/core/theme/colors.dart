import 'package:flutter/material.dart';

const kBg = Color(0xFF0F1117); // deep charcoal — page background
const kSurface = Color(0xFF1A1D27); // slate — cards, sheets, nav
const kSurface2 = Color(0xFF22263A); // elevated surface — modals, hover
const kAccent = Color(0xFFF59E0B); // amber/gold — primary CTA, active states
const kAccentDim = Color(0xFF7C5100); // muted amber — disabled, badges bg
const kSuccess = Color(0xFF22C55E); // green — available, success
const kWarning = Color(0xFFF59E0B); // amber — preparing, low stock
const kError = Color(0xFFEF4444); // red — error, urgent, occupied
const kInfo = Color(0xFF3B82F6); // blue — ready, info states
const kTextPrimary = Colors.white;
const kTextSecondary = Color(0xFFB0B7C3); // white70 equivalent
const kDivider = Color(0xFF2C3046); // subtle separator

const kRadiusCard = 12.0;
const kRadiusButton = 8.0;
const kRadiusSheet = 20.0; // bottom sheet top corners
const kRadiusChip = 20.0;
